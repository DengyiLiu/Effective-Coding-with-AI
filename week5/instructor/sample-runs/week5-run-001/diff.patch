diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/README.md instructor/demo/campus-room-booking-final/README.md
--- student/campus-room-booking/README.md	2026-06-29 02:17:49
+++ instructor/demo/campus-room-booking-final/README.md	2026-06-29 02:41:33
@@ -1,31 +1,33 @@
 # Campus Room Booking API
 
-This is the Week 5 starter project for AIM 5012.
+This is the Week 5 instructor demo final solution for AIM 5012.
 
 The API is intentionally small but realistic enough to practice controlled
 AI-assisted changes on an existing codebase.
 
 ## Current Behavior
 
-The starter already supports:
+This final solution supports:
 
 - list rooms
 - create confirmed bookings
-- reject overlapping bookings
+- create waitlisted bookings for overlapping requests
 - cancel bookings
+- promote the earliest waitlisted booking when a confirmed booking is cancelled
 - list bookings
 - run existing tests
 
-## Week 5 Feature Request
+## Implemented Week 5 Feature
 
-Add waitlist and cancellation promotion:
+This version implements waitlist and cancellation promotion:
 
 ```text
 When a requested room/time slot is already booked, create a waitlisted booking.
 When the confirmed booking is cancelled, promote the earliest waitlisted booking.
 ```
 
-Do not implement the feature before completing the workflow artifacts.
+The student starter project intentionally does not include this implementation.
+Use this copy for instructor rehearsal, comparison, or live demo recovery.
 
 ## Project Structure
 
@@ -41,6 +43,7 @@
   test_booking_create.py
   test_booking_cancel.py
   test_existing_behavior.py
+  test_waitlist.py
 scripts/
   verify.sh
 docs/
@@ -79,9 +82,9 @@
 
 ## Classroom Rule
 
-Do not ask AI to implement the feature immediately.
+Do not give this solution to students before they complete the workflow.
 
-Use the Week 5 workflow:
+Students should reach this behavior through the Week 5 workflow:
 
 ```text
 contract -> reconnaissance -> plan -> approval -> implementation
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/app/booking_service.py instructor/demo/campus-room-booking-final/app/booking_service.py
--- student/campus-room-booking/app/booking_service.py	2026-06-29 02:10:36
+++ instructor/demo/campus-room-booking-final/app/booking_service.py	2026-06-29 02:40:42
@@ -45,9 +45,10 @@
     if storage.get_room(room_id) is None:
         raise RoomNotFoundError(f"room '{room_id}' does not exist")
 
+    status = BookingStatus.CONFIRMED
     existing = _confirmed_bookings_for_room(room_id)
     if any(_times_overlap(start_time, end_time, booking.start_time, booking.end_time) for booking in existing):
-        raise BookingConflictError("room is already booked for that time slot")
+        status = BookingStatus.WAITLISTED
 
     booking = Booking(
         id=storage.next_booking_id(),
@@ -55,7 +56,7 @@
         requester=requester,
         start_time=start_time,
         end_time=end_time,
-        status=BookingStatus.CONFIRMED,
+        status=status,
         created_at=datetime.now(timezone.utc),
     )
     return storage.save_booking(booking)
@@ -69,10 +70,19 @@
     if booking.status == BookingStatus.CANCELLED:
         raise BookingAlreadyCancelledError(f"booking {booking_id} is already cancelled")
 
+    was_confirmed = booking.status == BookingStatus.CONFIRMED
     booking.status = BookingStatus.CANCELLED
-    return storage.save_booking(booking)
+    cancelled = storage.save_booking(booking)
 
+    if was_confirmed:
+        waitlisted = _earliest_waitlisted_for_same_slot(cancelled)
+        if waitlisted is not None:
+            waitlisted.status = BookingStatus.CONFIRMED
+            storage.save_booking(waitlisted)
 
+    return cancelled
+
+
 def list_bookings() -> list[Booking]:
     return storage.list_bookings()
 
@@ -87,6 +97,18 @@
         for booking in storage.list_bookings()
         if booking.room_id == room_id and booking.status == BookingStatus.CONFIRMED
     )
+
+
+def _earliest_waitlisted_for_same_slot(cancelled_booking: Booking) -> Booking | None:
+    candidates = (
+        booking
+        for booking in storage.list_bookings()
+        if booking.room_id == cancelled_booking.room_id
+        and booking.start_time == cancelled_booking.start_time
+        and booking.end_time == cancelled_booking.end_time
+        and booking.status == BookingStatus.WAITLISTED
+    )
+    return min(candidates, key=lambda booking: booking.id, default=None)
 
 
 def _times_overlap(
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/app/models.py instructor/demo/campus-room-booking-final/app/models.py
--- student/campus-room-booking/app/models.py	2026-06-29 02:10:35
+++ instructor/demo/campus-room-booking-final/app/models.py	2026-06-29 02:40:42
@@ -5,6 +5,7 @@
 
 class BookingStatus(str, Enum):
     CONFIRMED = "confirmed"
+    WAITLISTED = "waitlisted"
     CANCELLED = "cancelled"
 
 
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/docs/api-contract.md instructor/demo/campus-room-booking-final/docs/api-contract.md
--- student/campus-room-booking/docs/api-contract.md	2026-06-29 02:10:37
+++ instructor/demo/campus-room-booking-final/docs/api-contract.md	2026-06-29 02:41:33
@@ -1,7 +1,7 @@
 # API Contract
 
-This document describes the starter API behavior before the Week 5 feature is
-implemented.
+This document describes the instructor demo final solution after the Week 5
+feature is implemented.
 
 ## Rooms
 
@@ -28,19 +28,14 @@
 
 Returns booking records in id order.
 
-Booking statuses in the starter:
+Booking statuses:
 
 ```text
 confirmed
+waitlisted
 cancelled
 ```
 
-The Week 5 feature may add:
-
-```text
-waitlisted
-```
-
 ### POST /bookings
 
 Creates a confirmed booking when the requested room and time slot are available.
@@ -74,25 +69,31 @@
 }
 ```
 
-Starter conflict behavior:
+Implemented conflict behavior:
 
+```text
+An overlapping request for the same room/time should create a waitlisted
+booking instead of returning 409.
+```
+
+Example waitlist response:
+
 ```http
-409 Conflict
+201 Created
 ```
 
 ```json
 {
-  "detail": "room is already booked for that time slot"
+  "id": 2,
+  "room_id": "study-a",
+  "requester": "Bob",
+  "start_time": "2026-07-01T10:00:00Z",
+  "end_time": "2026-07-01T11:00:00Z",
+  "status": "waitlisted",
+  "created_at": "2026-07-01T14:01:00Z"
 }
 ```
 
-Week 5 feature target:
-
-```text
-An overlapping request for the same room/time should create a waitlisted
-booking instead of returning 409.
-```
-
 ### DELETE /bookings/{booking_id}
 
 Cancels an existing confirmed booking.
@@ -115,8 +116,6 @@
 }
 ```
 
-Week 5 feature target:
-
 ```text
 If a confirmed booking is cancelled and a waitlist exists for the same room and
 time slot, the earliest waitlisted booking should be promoted to confirmed.
@@ -134,12 +133,6 @@
 
 ```http
 400 Bad Request
-```
-
-Overlapping booking in starter:
-
-```http
-409 Conflict
 ```
 
 Already cancelled booking:
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/docs/architecture.md instructor/demo/campus-room-booking-final/docs/architecture.md
--- student/campus-room-booking/docs/architecture.md	2026-06-29 02:10:37
+++ instructor/demo/campus-room-booking-final/docs/architecture.md	2026-06-29 02:41:33
@@ -1,6 +1,6 @@
 # Architecture
 
-The starter project is intentionally small and layered.
+The final solution project is intentionally small and layered.
 
 ```text
 app/main.py
@@ -27,8 +27,8 @@
 -> validate requester and time range
 -> verify room exists
 -> find confirmed bookings for same room
--> reject overlapping confirmed booking
--> create confirmed booking
+-> create confirmed booking if no overlap exists
+-> create waitlisted booking if an overlap exists
 ```
 
 ## Current Cancellation Flow
@@ -39,6 +39,7 @@
 -> reject unknown booking
 -> reject already-cancelled booking
 -> mark booking cancelled
+-> if it was confirmed, promote earliest waitlisted booking for the same slot
 -> return updated booking
 ```
 
@@ -56,11 +57,12 @@
 10:00-11:00 and 11:00-12:00 do not overlap
 ```
 
-Cancelled bookings do not block future bookings.
+Cancelled bookings do not block future bookings. Waitlisted bookings also do
+not block future bookings; only confirmed bookings are blockers.
 
 ## Week 5 Feature Surface
 
-The likely feature area is:
+The implemented feature area is:
 
 ```text
 app/models.py
@@ -70,7 +72,7 @@
 docs/api-contract.md
 ```
 
-Students should not need to modify:
+The final solution does not modify:
 
 ```text
 scripts/verify.sh
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/tests/test_booking_create.py instructor/demo/campus-room-booking-final/tests/test_booking_create.py
--- student/campus-room-booking/tests/test_booking_create.py	2026-06-29 02:10:36
+++ instructor/demo/campus-room-booking-final/tests/test_booking_create.py	2026-06-29 02:40:42
@@ -33,14 +33,16 @@
     assert "created_at" in booking
 
 
-def test_rejects_overlapping_booking_for_same_room():
+def test_waitlists_overlapping_booking_for_same_room():
     first = client.post("/bookings", json=booking_payload(requester="Alice"))
     assert first.status_code == 201
 
     response = client.post("/bookings", json=booking_payload(requester="Bob"))
 
-    assert response.status_code == 409
-    assert response.json()["detail"] == "room is already booked for that time slot"
+    assert response.status_code == 201
+    booking = response.json()
+    assert booking["requester"] == "Bob"
+    assert booking["status"] == "waitlisted"
 
 
 def test_allows_adjacent_booking_for_same_room():
diff -ru -x .venv -x .pytest_cache -x __pycache__ student/campus-room-booking/tests/test_existing_behavior.py instructor/demo/campus-room-booking-final/tests/test_existing_behavior.py
--- student/campus-room-booking/tests/test_existing_behavior.py	2026-06-29 02:10:37
+++ instructor/demo/campus-room-booking-final/tests/test_existing_behavior.py	2026-06-29 02:40:42
@@ -41,18 +41,21 @@
     assert all(booking["status"] == "confirmed" for booking in bookings)
 
 
-def test_overlapping_request_is_rejected_and_does_not_create_second_booking():
+def test_overlapping_request_is_waitlisted_and_preserved():
     first = client.post("/bookings", json=booking_payload(requester="Alice"))
     assert first.status_code == 201
 
-    conflict = client.post("/bookings", json=booking_payload(requester="Bob"))
-    assert conflict.status_code == 409
+    waitlisted = client.post("/bookings", json=booking_payload(requester="Bob"))
+    assert waitlisted.status_code == 201
+    assert waitlisted.json()["status"] == "waitlisted"
 
     response = client.get("/bookings")
     bookings = response.json()
-    assert len(bookings) == 1
+    assert len(bookings) == 2
     assert bookings[0]["requester"] == "Alice"
     assert bookings[0]["status"] == "confirmed"
+    assert bookings[1]["requester"] == "Bob"
+    assert bookings[1]["status"] == "waitlisted"
 
 
 def test_room_listing_remains_available_after_booking_changes():
Only in instructor/demo/campus-room-booking-final/tests: test_waitlist.py
