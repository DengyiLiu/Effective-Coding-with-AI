# Waitlist Status Helper

This sample project is intentionally small. It connects a task contract, implementation files, tests, and a verification script.

The project contract is in:

```text
task-contract.yaml
```

The verification command is:

```bash
bash scripts/verify.sh
```

Expected result:

```text
Ran 15 tests
OK
```
